let a = '';

/* 
if (a) {
  console.log('true');
} else {
  console.log('false');
}
 */
/* 
const getName = (person) => {
  if(person === undefined || person === null) {
    return 'not an object';
  }

  return person.name;
};
 */

/* const getName = (person) => {
  if(!person) {
    return 'not an object';
  }

  return person.name;
};


let person = {name : '수빈'};
const name = getName(person);
console.log(name);
 */

// 단락회로 평가

/* 
const getName = (person) => {
  if(!person) {
    return "객체가 아닙니다.";
  }
  
  return person.name;

}

let person;
const name = getName(person);
console.log(name);

*/


















